# 📝 Initial-blog-app – тестове завдання KIT GLOBAL

Односторінковий блог на React + Firebase. Дозволяє створювати, переглядати, редагувати та видаляти пости, а також залишати коментарі. Фільтрація за автором реалізована на клієнті, збереження даних – у Firestore.

---

## 🔧 Стек

- **React 19**, **TypeScript**, **Vite**
- **React Router 7** – маршрутизація сторінок
- **Redux Toolkit** – кеш стану, асинхронне завантаження
- **SWR** – оптимістичний фетч та кеш клієнта
- **Firebase Firestore** – база даних
- **React Hook Form + Zod** – форми та валідація
- **TailwindCSS** + кастомні UI-компоненти

---

## 🏗️ Архітектура та потоки даних

- `src/app` – конфігурація Redux store + typed hooks для використання в компонентах.
- `src/features/posts` – slice з `createAsyncThunk(fetchPosts)` та редюсер станів завантаження/помилки.
- `src/services/firebase.ts` – обгортки над Firestore (CRUD для постів, коментарів, дата-хелпери).
- `src/hooks/usePostsSWR.ts` – клієнтський SWR-фетчер, що синхронізує локальний список постів із Firestore.
- `src/pages` – сторінки маршрутизатора:
  - `HomePage` – список постів із фільтром автора, карточки клікабельні.
  - `PostPage` – детальний перегляд та `CommentsSection`.
  - `CreatePostPage` / `EditPostPage` – форми з валідацією Zod.
- `src/components/ui` – базові контролли (`Card`, `Button`, `Input`) з Tailwind-стилями.
- `src/routes/AppRouter.tsx` – декларація маршрутів SPA.
- `src/utils/postSchema.ts` – Zod-схема форми, тип `PostFormData`.

Завантаження даних виконується у двох режимах:

1. **SWR** для HomePage – швидкий кеш + повторне опитування без ручного контролю стану.
2. **Redux Toolkit** (`fetchPosts`) – можливість централізовано кешувати список постів для інших частин застосунку.

---

## 📂 Структура директорій

```
src/
├── app/            # Redux store та типізовані хуки
├── components/     # UI та семантичні блоки (CommentsSection, Card, Button, Input)
├── features/       # Slice-и стану (posts)
├── hooks/          # Кастомні хуки (usePostsSWR)
├── pages/          # Реалізація маршрутів (Home, Post, Create, Edit)
├── routes/         # Налаштування React Router
├── services/       # Firebase Firestore CRUD
├── types/          # Спільні інтерфейси Post, Comment
├── utils/          # Zod-схеми й допоміжні функції
├── assets/         # SVG/іконки
└── index.css       # Глобальні стилі та Tailwind layer-и
```

---

## 🚀 Демо та репозиторій

- Live: [https://initial-blog-giv8r88j6-ruslans-projects-362af729.vercel.app](https://initial-blog-giv8r88j6-ruslans-projects-362af729.vercel.app)  
- GitHub: [https://github.com/RusVass/Initial-blog-app](https://github.com/RusVass/Initial-blog-app)

---

## ▶️ Запуск локально

```bash
git clone https://github.com/RusVass/Initial-blog-app.git
cd Initial-blog-app
npm install
npm run dev
```

### Обовʼязкові змінні середовища (`.env`)

```
VITE_FIREBASE_API_KEY=...
VITE_FIREBASE_AUTH_DOMAIN=...
VITE_FIREBASE_PROJECT_ID=...
VITE_FIREBASE_STORAGE_BUCKET=...
VITE_FIREBASE_MESSAGING_SENDER_ID=...
VITE_FIREBASE_APP_ID=...
```

- Змінні додаються у `.env` (локально) та у Vercel Dashboard (прод).
- Для продакшн-збірки: `npm run build && npm run preview`.
- Лінтинг перед PR: `npm run lint`.

---

## 🧪 Тести

Тестовий раннер – **Vitest**:

```bash
npm run test       # одноразовий прогін
npm run test:watch # режим спостереження
```

Покриті сценарії:

- `src/utils/__tests__/postSchema.test.ts` – валідація форм (успіх/помилки полів).
- `src/features/posts/__tests__/postsSlice.test.ts` – стани `pending/fulfilled/rejected` для `fetchPosts`.

```ts
// фрагмент postsSlice тестів
expect(state.loading).toBe(false)
expect(state.posts).toEqual(mockPosts)
expect(state.error).toBeNull()
```

Додаткові тести можна легко писати для компонентів, адже Vitest налаштований на `jsdom`.

---

## ✅ Реалізовано

- CRUD-операції для постів + коментарі.
- SWR + Redux Toolkit для керування даними.
- Валідація форм через Zod та React Hook Form.
- Адаптивний інтерфейс (Tailwind + кастомні UI-компоненти).
- Деплой на Vercel з автоматичним CI/CD.

---

## 🧑‍💻 Автор

Проєкт виконано як тестове завдання для KIT GLOBAL.  
Автор: **Ruslan Vasiliev**
